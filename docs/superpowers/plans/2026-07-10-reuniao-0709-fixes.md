# Reunião 09/07 (2h10) — Correções e Features Implementation Plan

> **Para quem for executar:** REQUIRED SUB-SKILL: use superpowers:subagent-driven-development (recomendado, tarefas independentes) ou superpowers:executing-plans (execução inline em sessão única). Este projeto NÃO tem suite de testes unitários -- verificação é sempre `npx tsc --noEmit -p .` (obrigatório, zero erros) + QA ao vivo (dev server, ou consulta direta no banco via `node scripts/db.mjs`) antes de considerar uma tarefa pronta. Onde a skill padrão pede "escreva o teste que falha", leia como "abra a tela/rota e reproduza o bug ANTES de mexer, pra confirmar causa raiz".

**Goal:** Resolver os 17 itens levantados na reunião de desenvolvimento de 09/07/2026 (2h10, ntb-estoque + 1 item cross-sistema ntb-vendas) e fechar a limpeza de OPs fantasmas já em andamento.

**Architecture:** Next.js 16 App Router + Supabase + Omie ERP (mesmo padrão usado em todo o repo hoje: Server Components pra leitura, Server Actions pra escrita, `FiltrosGaveta`/`MultiSelect` pro sistema de filtros, migrations numeradas em `supabase/migrations/`).

**Tech Stack:** TypeScript, Next.js 16, Supabase (Postgres), Omie API (`lib/omie/*`), `components/ui-kit/*`.

## Global Constraints

- Zero travessão (—) em qualquer texto visível ao usuário.
- `npx tsc --noEmit -p .` limpo antes de considerar qualquer tarefa pronta.
- Loja 4 ("O SERTÃO VAI VIRAR MAR") é produção real -- nunca testar ao vivo nela. Usar loja 3 (Donana Rio Vermelho) ou 7 (VINHAS).
- Migrations aplicadas via `node scripts/aplicar-migration.mjs <arquivo>.sql` (não CLI Supabase).
- Rate limit Omie: 240 req/min, ~300-400ms entre leituras.
- Server Components que precisam de `valoresMulti`/`CampoFiltro` importam de `@/components/ui-kit/filtros-utils` (NUNCA de `Filtros.tsx`, que é `'use client'` -- crasha em runtime, não pega no typecheck).

---

## Visão geral por fase

| Fase | Conteúdo | Itens | Risco | Depende de |
|---|---|---|---|---|
| 0 | Fechar limpeza de OPs fantasmas (já em andamento) | #14 | Baixo | — |
| 1 | Bugs já diagnosticados com vídeo (precisos, rápidos) | #16, #22, #24, #30 | Baixo | — |
| 2 | Resumo do Dia → Resumo Operacional | #25, #26 | Médio | Fase 1 (mesmo arquivo lib/resumo-dia.ts) |
| 3 | Relatórios: CFOP de entrada + faturamento | #27, #28, #31 | Médio | — |
| 4 | Movimentações: filtros + criar movimento manual | #21 | Médio-Alto | Fase 1 (bug #22 resolvido primeiro, mesma tela) |
| 5 | Etiqueta: ajuste visual de código de barras/QR | #17 | Baixo | — |
| 6 | OP: lembrete de etiqueta + alerta de estoque | #18, #20 | Médio | — |
| 7 | OP: rastreio de lote (FIFO) -- **precisa de brainstorming, não codar direto** | #19 | Alto | Fase 6 |
| 8 | Cadastro de produto: subprodutos cross-sistema (ntb-vendas) | #23 | Alto | — |
| 9 | NF Entrada: cadastro de categorias contábeis | #29 | Médio | — |
| — | **Bloqueado**: NF Entrada com auto-associação CFOP/CST/ICMS | #11 | — | Slide de correlação do Ramon (não recebido) |

**Ordem recomendada:** 0 → 1 → 2 → 3 → 5 (rápidos e independentes) → 4 → 6 → 8 → 9 → 7 (o mais arriscado por último, precisa de alinhamento).

---

## Fase 0 — Fechar limpeza de OPs fantasmas

| Task | Arquivos | O que fazer | Como verificar |
|---|---|---|---|
| 0.1 | `lib/omie/ordem-producao.ts` (já editado, não commitado) | Já criei `reconciliarOPsFantasmas()`. Confirmar que o script `_qa_op_limpar_fantasmas2.mjs` em background terminou as 24 OPs restantes. | `node scripts/db.mjs "SELECT count(*) FROM ordens_producao WHERE loja_id=6 AND created_at='2026-06-20T11:19:43.839Z' AND concluida=false"` -- deve cair pra só as REAIS confirmadas |
| 0.2 | `app/api/cron/sync-reconciliar-op/route.ts` (já criado, não commitado) | Revisar o arquivo, rodar `npx tsc --noEmit -p .` | Typecheck limpo |
| 0.3 | `vercel.json` | Adicionar entrada de cron pro `sync-reconciliar-op` (ainda não adicionado) | Ver `vercel.json` tem a linha nova |
| 0.4 | commit | `git add lib/omie/ordem-producao.ts app/api/cron/sync-reconciliar-op vercel.json && git commit` | — |
| 0.5 | Task #14 (código de OP diferente do Omie) | Depois da reconciliação completa, checar se sobrou algum caso "REAL" com código genuinamente errado (não fantasma) -- se sim, investigar caso a caso; se não, fechar #14 como resolvido pela reconciliação | Ver output final do script + `node scripts/db.mjs` |

---

## Fase 1 — Bugs já diagnosticados com vídeo

| Task | Arquivo(s) | O que fazer | Como verificar |
|---|---|---|---|
| 1.1 (#16) | `app/(app)/nota-fiscal/[id]/imprimir/route.ts` | Reproduzir: abrir uma NF com item de quantidade fracionária e tentar imprimir etiqueta (`?itens=...`). Achar onde valida "quantidade definida para etiqueta" e corrigir pra aceitar fracionário, ou dar erro claro (não JSON cru) explicando o que falta preencher. | Reproduzir o erro original primeiro (confirma causa raiz), depois testar com item fracionário e com item sem quantidade definida (esse SIM deve dar erro, mas com mensagem de tela, não JSON cru) |
| 1.2 (#24) | `app/(app)/inventario/page.tsx` | Comparar com uma tela que já ordena por clique no cabeçalho (ex: `app/(app)/nota-fiscal/page.tsx`) e replicar o padrão nas colunas LOCAL, DATA, RESPONSÁVEL, INTEGRADOS, STATUS | Abrir `/inventario`, clicar em cada cabeçalho, confirmar que ordena asc/desc |
| 1.3 (#22) | `app/(app)/movimentacoes/page.tsx`, `components/movimentacoes/HistoricoTab.tsx`, `components/movimentacoes/MovimentosTab.tsx` (ou nomes equivalentes) | Reproduzir com produto código 80000, período 01/06 a 30/06/2026: comparar a query da aba Histórico com a da aba Movimentos pro mesmo produto/período, achar a divergência (fonte de dado diferente? filtro de status diferente?) | Reproduzir o cenário exato da reunião, confirmar números batem depois do fix |
| 1.4 (#30) | `app/(app)/auditoria-fiscal/page.tsx` | O clique na linha da tabela CFOP muda a URL (`?cfop=X`) mas a tabela não filtra -- achar onde o searchParam deveria ser lido/aplicado na query e corrigir | Clicar numa linha de CFOP, confirmar que a tabela filtra só aquele CFOP |

---

## Fase 2 — Resumo do Dia → Resumo Operacional

| Task | Arquivo(s) | O que fazer | Como verificar |
|---|---|---|---|
| 2.1 (#25) | `app/(app)/resumo/page.tsx`, `lib/resumo-dia.ts`, `components/resumo/ResumoFiltros.tsx` | Adicionar filtro de período (dia/semana/mês) que afete TODAS as categorias do resumo, não só a auditoria (que já tem desde hoje de manhã). Considerar renomear título pra "Resumo Operacional". | Trocar entre dia/semana/mês e ver os números de cada card mudarem de verdade |
| 2.2 (#26) | `app/(app)/resumo/page.tsx` | Remover o card "Movimentações" (redundante, já existe em `/movimentacoes`). Filtrar o card "Produção" pra só contar produto em processo/incluído. | Confirmar visualmente que o card sumiu e que Produção só conta o tipo certo |
| 2.3 (#26, investigação extra) | — | Investigar achado do vídeo: loja 6 (Donana Brotas), 08/07/2026, 91 produções concluídas e 0 etiquetas impressas no card. Consultar `impressao_etiquetas` pra esse dia/loja e confirmar se é comportamento real (ninguém imprimiu mesmo) ou contador quebrado. | `node scripts/db.mjs "SELECT count(*) FROM impressao_etiquetas WHERE loja_id=6 AND created_at::date='2026-07-08'"` |

---

## Fase 3 — Relatórios: CFOP de entrada + faturamento + navegação

| Task | Arquivo(s) | O que fazer | Como verificar |
|---|---|---|---|
| 3.1 (#27) | `app/(app)/relatorio-compras/page.tsx`, `lib/cfop.ts` | Trocar a fonte do CFOP mostrado: hoje é o CFOP do fornecedor, tem que ser o CFOP DE ENTRADA (o que a NF realmente usou). Completar o mapeamento em `lib/cfop.ts` pros CFOPs que hoje caem em "não catalogado" (43,6% do valor no período visto na auditoria fiscal -- puxar a lista real de CFOPs não catalogados do banco antes de mapear) | `node scripts/db.mjs` pra listar CFOPs distintos não mapeados; depois conferir que a % de "não catalogado" caiu no relatório |
| 3.2 (#28) | `app/(app)/relatorio-faturamento/page.tsx` | Investigar por que "produto acabado" às vezes cai como "não classificado". Reordenar exibição: produto acabado primeiro, revenda depois, outros por último. | Reproduzir com dado real de faturamento, confirmar classificação e ordem |
| 3.3 (#31) | Todas as páginas de relatório sem botão voltar (`relatorio-compras`, `relatorio-estoque-valorizado`, `relatorio-faturamento`, `relatorio-indicadores`, `relatorio-margem`, `relatorio-movimentacao`, `auditoria-fiscal`) | Adicionar botão de voltar consistente (reaproveitar componente já usado em alguma tela, se existir; senão criar um pequeno componente compartilhado) | Navegar por cada relatório e confirmar o botão funciona |

---

## Fase 4 — Movimentações: filtros e criação manual

| Task | Arquivo(s) | O que fazer | Como verificar |
|---|---|---|---|
| 4.1 (#21) | `app/(app)/movimentacoes/page.tsx` | Adicionar filtro de local de estoque e período na aba de histórico por produto (hoje falta) | Filtrar por local específico e confirmar que só mostra movimentos daquele local |
| 4.2 (#21) | idem | Adicionar saldo inicial e saldo final na lista de histórico | Conferir contra o saldo real do produto na tela de Produtos |
| 4.3 (#21) | `app/(app)/movimentacoes/page.tsx`, `lib/actions/movimentacoes.ts` (criar se não existir) | Permitir CRIAR um novo movimento (ajuste de saldo, transferência) direto pela tela de movimentação, não só visualizar | Criar um ajuste de teste na loja 3 ou 7, confirmar que reflete no saldo e no Omie |

---

## Fase 5 — Etiqueta: ajuste visual

| Task | Arquivo(s) | O que fazer | Como verificar |
|---|---|---|---|
| 5.1 (#17) | `components/etiqueta/EtiquetaPDF.tsx` | Aumentar a proporção do QR/código de barras em relação ao texto (dentro do `buildStyles`, ajustar `qrSize`/posição). Reaproveitar o padrão de teste visual usado no fix de fonte de hoje (`_qa_etiqueta_*.mjs`, gerar PDF real e ler como imagem antes de confirmar) | Gerar etiqueta de teste, comparar visualmente antes/depois igual fiz hoje de manhã com a fonte |

---

## Fase 6 — OP: lembrete de etiqueta + alerta de estoque

| Task | Arquivo(s) | O que fazer | Como verificar |
|---|---|---|---|
| 6.1 (#18) | `components/ordem-producao/OrdemProducaoRow.tsx` (`concluir()`), `lib/actions/ordem-producao.ts` (`finishOP`) | Depois de concluir uma OP com sucesso, se nenhuma etiqueta foi impressa pra ela, mostrar um toast/lembrete sugerindo imprimir (não bloquear a conclusão) | Concluir uma OP de teste sem imprimir etiqueta, confirmar que aparece o lembrete |
| 6.2 (#20) | `components/ordem-producao/OrdemProducaoRow.tsx` (`concluir()` ou na criação da OP), `lib/actions/ordem-producao.ts` | Antes de criar/concluir uma OP, se já existe saldo do produto em estoque, mostrar alerta "você já tem X em estoque, tem certeza que quer produzir mais?" (não bloquear, só confirmar) | Testar com produto que já tem saldo positivo, confirmar que o alerta aparece |

---

## Fase 7 — OP: rastreio de lote (FIFO) — precisa de brainstorming primeiro

| Task | O que fazer |
|---|---|
| 7.1 | **NÃO implementar direto.** Rodar `superpowers:brainstorming` com o Ramon/André pra decidir o desenho: vincular ordem de produção na transferência entre locais? Exigir leitura de lote no consumo? Alerta inteligente só, sem rastreio de verdade? Extrapola o escopo desta reunião -- eles mesmos disseram "vamos pensar um pouquinho" sem decidir. |

---

## Fase 8 — Cadastro de produto: subprodutos cross-sistema

| Task | Arquivo(s) | O que fazer | Como verificar |
|---|---|---|---|
| 8.1 (#23) | ntb-estoque: tela de cadastro de produto (`app/(app)/produtos/...`), migration nova pra relação subproduto | Ao marcar produto como "frente de loja", permitir vincular subprodutos/adicionais com quantidade | Cadastrar um produto de teste com subproduto vinculado |
| 8.2 (#23) | ntb-vendas: `lib/api.ts`, migration nova | Consumir essa relação vinda do ntb-estoque pra criar automaticamente os adicionais correspondentes no cardápio, com a quantidade certa pra Ordem de Produção sair certa | Testar fluxo completo: cadastra no Stock -> aparece certo no Vendas -> gera OP com quantidade certa |

**Atenção:** esta fase mexe nos DOIS repositórios (ntb-estoque-next e ntb-vendas). Coordenar as duas partes antes de subir uma sozinha.

---

## Fase 9 — NF Entrada: cadastro de categorias contábeis

| Task | Arquivo(s) | O que fazer | Como verificar |
|---|---|---|---|
| 9.1 (#29) | Migration nova (`categorias_contabeis`), tela de cadastro nova | Criar tela de cadastro de categorias (compra mercadoria revenda, matéria-prima, serviços, uso/consumo, embalagem, material de escritório etc), espelhando as categorias contábeis do Omie mostradas na reunião | Cadastrar as categorias vistas no vídeo, confirmar que aparecem pra escolher no lançamento de NF |

---

## Bloqueado — não iniciar

| # | Item | Motivo |
|---|---|---|
| 11 | NF Entrada com auto-associação de CFOP/CST/crédito de ICMS (ntb-vendas) | Precisa do slide com a tabela de correlação da contabilidade que o Ramon disse que ia mandar. É fiscal -- não posso inventar a regra. |

---

## Self-review (cobertura da reunião)

Todos os 17 itens levantados na reunião de 09/07 (tasks #14 e #16-#31 do meu tracking) têm task correspondente numa fase acima. Nenhum placeholder tipo "adicionar validação apropriada" sem dizer qual -- cada task tem arquivo e ação concretos, exceto a Fase 7 (rastreio de lote), que é DELIBERADAMENTE um brainstorming, não uma implementação, porque a própria reunião terminou sem decisão de desenho.
